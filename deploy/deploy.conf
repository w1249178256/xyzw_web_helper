# 阿里云服务器 & ACR 部署配置
# 被 deploy/ 下所有脚本共用

# ---- 服务器 SSH ----
REMOTE_USER="root"
REMOTE_HOST="47.120.18.209"
REMOTE_PORT="22"

# ---- 阿里云 ACR ----
ACR_REGISTRY="crpi-ijyip3mvrstcfvh2.cn-heyuan.personal.cr.aliyuncs.com"       # 公网（本地 push）
ACR_REGISTRY_VPC="crpi-ijyip3mvrstcfvh2-vpc.cn-heyuan.personal.cr.aliyuncs.com" # VPC（服务器 pull）
ACR_NAMESPACE="agnosis"
ACR_USERNAME="agnosis@foxmail.com"

# ---- 数据库备份 ----
REMOTE_DB_PATH="/root/xyzw/data/xyzw.db"      # 服务器宿主机上的数据库路径（bind mount）
BACKUP_LOCAL_DIR="${SCRIPT_DIR}/database"       # 本地备份存放目录
BACKUP_KEEP_DAYS=30                            # 保留最近 N 天的备份
